Full-bleed case imagery goes here.

One file per case, named exactly as the `imageAsset` field in `../cases.json`.
Portrait or square source is preferred; the pager crops to cover, so a landscape
frame loses its edges.

Nothing here yet. Until real endoscopy images land, the pager falls back to a
neutral placeholder and the layout stays intact.
